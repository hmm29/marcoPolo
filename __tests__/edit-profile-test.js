/**
 * Created by harrisonmiller on 5/18/15.
 */
'use strict';

jest.dontMock('../App/Components/EditProfile');

var EditProfile = require('../App/Components/EditProfile');

describe('_onTyping', () => {
    it ('returns a list of genders that start with text', (text) => {
        expect()
    });
});